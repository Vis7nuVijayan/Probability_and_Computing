
public class Solution
{
	static double[] getRandomRating(int n)
	{
		double temp[] = new double[n];
		for(int i=0; i<n; ++i)
			temp[i] = Math.random();
		return temp;
	}
	
	public static void main(String args[])
	{
		double[] rating = getRandomRating(1000);
		for(int i=0; i<3; ++i)
			System.out.print((int)Math.random()*1000+" ");
		System.out.println();
	}
}
